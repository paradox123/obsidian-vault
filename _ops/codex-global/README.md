# Codex Global Ops

This workspace is the explicit home for machine-level Codex automations.

Use it for automations that inspect global Codex state, such as session history,
automation memory, and shared skill improvements. Do not use the user home
directory as an automation `cwd` when the automation is expected to write files.

Project-specific automations should stay anchored to the repositories they
inspect or edit.
